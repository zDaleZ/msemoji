You can get the source code of msemoji from here.



To generate script, `node`, `npm` and `yarn` are required.

To generate regex, `php` is required.

Everything should be work by executing `yarn run generate`.
